// @ts-nocheck

/** Renders the fallback route when no client-side route matches. */
export function NotFoundView() {
  return <section class="view"><div class="empty-state">Page not found.</div></section>;
}
